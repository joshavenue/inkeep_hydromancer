[pytest]
addopts = --basetemp=./.test-tmp
markers =
    full_corpus: opt-in historical public snapshot validation (--full-corpus)
    protocol: opt-in temporary loopback MCP SDK test (--run-protocol)
